import { StyleSheet } from "react-native";

export const CustomImageCss = StyleSheet.create({
    container: {
      justifyContent: 'center',
      alignItems: 'center',
    },
    image: {
      width: 100,
      height: 100,
    },
  });